name: comcolapp
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: 
  equatable: 
  camera: 
  tflite_flutter: 
  yaml: 
  get_it: 
  injectable: 
  go_router:
  image:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: 
  injectable_generator:

flutter:
  uses-material-design: true

  assets:
    - assets/models/
